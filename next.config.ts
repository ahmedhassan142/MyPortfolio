/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // Remove all experimental features unless absolutely necessary
  // Keep the config as simple as possible
}

module.exports = nextConfig